<UserControl
    x:Class="CODuo.Controls.RegionGenerationChart"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CODuo.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Viewbox>
            <Canvas Width="{x:Bind Path=ChartWidth}" Height="{x:Bind Path=ChartHeight}">
                <Path x:Name="Coal" />
                <Path x:Name="Oil" />
                <Path x:Name="Gas" />
                <Path x:Name="Import" />
                <Path x:Name="Other" />
                <Path x:Name="Biomass" />
                <Path x:Name="Nuclear" />
                <Path x:Name="Solar" />
                <Path x:Name="Hydro" />
                <Path x:Name="Wind" />
            </Canvas>
        </Viewbox>
    </Grid>
</UserControl>
